@model Note

@{
    ViewData["Title"] = "Create Task";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <h2>Create New Task</h2>
            <form method="post">
                <div class="form-group mb-3">
                    <label for="Title">Title</label>
                    <input type="text" class="form-control" id="Title" name="Title" placeholder="Task title" required>
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label for="Content">Description</label>
                    <textarea class="form-control" id="Content" name="Content" rows="5" placeholder="Task description" required></textarea>
                    <span asp-validation-for="Content" class="text-danger"></span>
                </div>

                @if (!ViewData.ModelState.IsValid)
                {
                    <div class="alert alert-danger">
                        @Html.ValidationSummary()
                    </div>
                }

                <button type="submit" class="btn btn-primary">Create Task</button>
                <a href="@Url.Action("Index", "Notes")" class="btn btn-secondary">Cancel</a>
            </form>
        </div>
    </div>
</div>
